<div class="hero-section">
	<div class="hero-wrap global-padding">
		<h1 class="hero-title global-title">{{#if @custom.hero_title_text}}{{{@custom.hero_title_text}}}{{else}}<strong>Welcome to the Studio 🎨</strong><br>Journals on the journey and the human experience.{{/if}}</h1>
		{{>search is_hero=true}}
		{{>members/subscribe_form is_hero=true}}
	</div>
</div>